
THESE ARE MY PERSONAL NOTES (can be used by contributors to see what needs to be done)

Build dev env:
docker compose -f docker-compose.dev.yml --project-name livednd-dev up --build 

Plan for live-dnd-list:

(to fix frontend)
- add fast navigation to class/race/backgr cards in wirard (buttons in header)
- bug with proficiency of sleight of hand skill
- caching added to new parts and rewritten to redis
- make active weapons a scrollable horizontally on mobile
- fix SetActive button when adding a weapon to work when save button is pressed and layout on mobile
- add overall field validation
- add current hits validation (not higher than max hits)
- refactor spell search
- add UI to change character logo
- add race, class, etc. pop-up windows with desc
- add theme change based on base-16 color scheme (or another type of choice)

(Quality of life)
- add swipe (left <- right) to open side-menu with labels for fast sheet traverse on mobile
- when logged in - open last opened character

(later plans)
- why do races still use ResponseEntity ???: 
- rewrite my Cache Manager -> Redis
- add export to pdf via async method
- get the rest of info fetched from open5e
- add sharing feature?
- add likes to lists feature?
- add homebrew functionality/validation
- split endpoints for admin and user
- enhance design (animations, cool backgrounds, etc.) // half-way done

(afterwards)
- determine the next system
- implement system choise


IMPORTANT NOTES:
- currently speed is not being derived from race
- currently I cannot add more than 1 class to character

(current fixes:)
LEVELING
    - currently levelup -> new class does not work + no sync with exp
- rewrite character creation to be multistaged with info about class/race/background provided:
    - Cantrips Known/Ki Points/etc. for some reason can be duplicated with differing values // weird
    - game system choice & validation
    - better font families
    - if no desc - just ignore decs DO NOT WRITE [no description provided] // can't find where I found it
- add multiclassing/leveling mechanic
- add feat of watching in separate window (or something similar) info of classes/races/backgrounds;
